import { FilterbycatPipe } from './filterbycat.pipe';

describe('FilterbycatPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterbycatPipe();
    expect(pipe).toBeTruthy();
  });
});
